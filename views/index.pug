//- extends layout

//- block content
//-   h1= title
//-   p The unicorn is the national animal of Scotland. #{title}
//-   button(type="button", class="btn btn-secondary btn-lg" onclick="update()") Press me
//-   div(id="target" class="container text-center m-2 p-3" style="color:white;font-weight:700; background-color:plum;") Display
//-   script. 
//-       var client_count=0 
//-       function update(){
//-         client_count++;
//-         document.getElementById("target").innerHTML=`Value now ${client_count}`
//-       } 
//-   a(href="/bags") go to bags

extends layout
block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/bags") Take me to bags
    br
    a(href="/logout") Logout